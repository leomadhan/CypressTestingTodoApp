# React Todo WebApp tested via Cypress

Going to run as first time. Please run the following command
```sh
npm install
```

To open Cypress runner window / To run test spec on select
```sh
npm cypress:open
```

To run cypress thru CLI
```sh
npm testCli
```
